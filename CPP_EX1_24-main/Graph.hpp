#include <iostream>
#include <vector>

namespace ariel {
    class Graph {

        public:

            // Graph() ()

            void loadGraph(vector<vector<int>> graph);
            void printGraph();

    };
}